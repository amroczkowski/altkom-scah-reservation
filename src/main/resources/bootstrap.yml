spring:
  application:
    name: reservation-service
  cloud:
    config:
      enabled: true
      uri: http://altkom-scah-config-server:8080
      fail-fast: true
      retry:
        max-attempts: 20
